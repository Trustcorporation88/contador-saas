# Em produção o frontend fala com a própria origem: o rewrite /api em
# frontend/vercel.json encaminha para o backend no Railway. Deixar
# VITE_API_URL vazio mantém esse comportamento (e evita CORS).
#
# Só preencha para apontar o build para uma API em outro domínio.
VITE_API_URL=
