# Template local do Vercel CLI — NÃO contém secrets.
# Em produção o frontend usa same-origin (api.ts) e o rewrite em
# frontend/vercel.json aponta /api → contador-api (Railway).
VITE_API_BASE_URL=
VITE_API_URL=
