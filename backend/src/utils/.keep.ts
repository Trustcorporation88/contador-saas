/**
 * Utils directory
 * Helper functions and utilities
 *
 * Example structure:
 * - validators.ts       (Joi schemas)
 * - formatters.ts       (date, currency formatting)
 * - encryption.ts       (password hashing, etc)
 * - dateUtils.ts        (date calculations)
 * - mathUtils.ts        (accounting calculations)
 * - errorUtils.ts       (custom error classes)
 */

export default {};
