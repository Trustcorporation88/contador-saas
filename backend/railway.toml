# Backend API — deploy no Railway (substitui Render)
# No Railway: Root Directory = backend (ou use railway up . --path-as-root na pasta backend)

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 180
restartPolicyType = "ON_FAILURE"
